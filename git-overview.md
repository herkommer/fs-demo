git version
git init
git status

- untracked
- modified
- staged
  git add
- lägger till filen till stage (index)
- test.txt
- -A
- .
  git commit
- öppnar editorn för att ange meddelande för commit
- -m "meddelande"
- -am "meddelande" (denna gör Add -A och Commit)
- --amend (justera det sista meddelandet)
  git log
- --oneline
  git diff
- --staged
- HEAD~3 (jmf 3 versioner bakåt med aktuell version)
  git clone URL/sökväg
  git remote
- add
- remove
- show
- -v
  git push
  git pull
  git branch
- -v
  git checkout
- -b (skapa och växla)
  git switch
  git show
  git blame
